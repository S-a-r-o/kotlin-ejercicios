var numero = 1

// Ciclo while para imprimir números pares
while (numero <= 20) {
    // Verificamos si el número es par
    if (numero % 2 == 0) {
        // Imprimimos el número si es par
        println(numero)
    }
    // Incrementamos el número en 1
    numero++
}
