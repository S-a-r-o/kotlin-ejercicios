val originalString = "Hola, Kotlin es genial!"

// Define los índices para extraer la subcadena
val startIndex = 7
val endIndex = 13

// Extrae la subcadena usando el método substring
val subString = originalString.substring(startIndex, endIndex)

// Imprime la subcadena
println("La subcadena es: $subString")
